---@enum EATMTimeUpdateSource
local EATMTimeUpdateSource = {
    FROM_INIT = 0,
    FROM_TICK = 1,
    FROM_GAMEPLAY = 2,
    FROM_DEBUG = 3,
    FROM_MAX = 4,
}

---@enum EPropertyApplication
local EPropertyApplication = {
    Override = 0,
    Addition = 1,
    Multiplication = 2,
    EPropertyApplication_MAX = 3,
}

